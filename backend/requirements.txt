# System dependency required for video uploads:
# Install the ffmpeg binary with your OS package manager, not with pip.
# Example (Ubuntu/WSL): sudo apt install ffmpeg

# Runtime dependencies
fastapi==0.135.2
uvicorn[standard]==0.38.0
python-multipart==0.0.20
python-dotenv==1.2.2
google-genai==1.66.0
tavily-python==0.7.12
requests==2.31.0

# Dev/test dependencies
pytest==8.4.2
